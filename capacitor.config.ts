import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-proj-template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
